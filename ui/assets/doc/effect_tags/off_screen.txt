[b]小对话框（画外音）[/b]

在当前位置插入一个小对话框。

用法：
[lb]off_screen face=example text=example/]

参数：
• [b]face[/b]：脸图id
• [b]text[/b]：对话文本（不支持bbcode标签）

注意：当前版本建议放在需要出现小对话框的位置的前一个字符处。

小对话框可以表示其他角色的反应。

示例：
主要对话内容[lb]off_screen face=spr_face_susie_0 text=哇！真的吗？/]
